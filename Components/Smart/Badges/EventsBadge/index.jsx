import React from 'react';
import PropTypes from 'prop-types';
import UseStyles from './styles';

const EventsBadge = (props) => {
    const styles = UseStyles({}, {link: true});

    return (
        <div></div>
    )
}
EventsBadge.propTypes = {};
export default React.memo(EventsBadge);